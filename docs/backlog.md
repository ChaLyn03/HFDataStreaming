# Backlog

- Add SSE/WebSocket endpoint for live alerts.
- Add role-based access control (admin/read-only).
- Swap SQLite for Postgres and add migrations.
- Add Prometheus-style `/metrics` endpoint.
- Add analytics service for weekly/monthly statistics.
- Add CI pipelines for container builds and vulnerability scanning.
